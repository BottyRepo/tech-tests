# Front-End Specialist — Technical Interview

---

## Питання 1 — JavaScript / Event Loop

Є два фрагменти коду. Кожна функція виконує **математичні обчислення** (без читання з диску, без мережевих запитів) і займає **~10 секунд** синхронного виконання.

```js
function heavyCalc1() {
  return new Promise((resolve) => {
    let result = 0;
    const end = Date.now() + 10_000;
    while (Date.now() < end) {
      result += Math.sqrt(Math.random());
    }
    resolve(result);
  });
}

function heavyCalc2() {
  return new Promise((resolve) => {
    let result = 0;
    const end = Date.now() + 10_000; 
    while (Date.now() < end) {
      result += Math.sqrt(Math.random());
    }
    resolve(result);
  });
}

const [r1, r2] = await Promise.all([heavyCalc1(), heavyCalc2()]);
```

### Питання до кандидата:

1. Скільки секунд займе виконання `Promise.all` — **~10 сек чи ~20 сек**? Чому?
2. Чи виконуються ці дві функції **паралельно**? Поясни механізм.
3. Що таке **event loop** і як він пов'язаний з цією поведінкою?
4. Як можна було б зробити **справжнє паралельне** виконання цих обчислень у браузері?

---

## Питання 2

> Прикріплена картинка з макетом / ТЗ:

<img src="./form.png" width="70%" alt="Макет завдання" />

---

**Завдання:** Зробити форму з полями вводу з 5 інпутами:
- Ім'я
- Логін
- Пароль
- Дата народження

---

### Частина A — Уточнюючі питання

*Скажи всі питання, які у тебе є перед початком розробки?*

---

### Частина B — Припущення та оцінка

*Озвуч припущення — відповіді на свої питання. З урахуванням цих припущень:*

1. **Оціни час** на виконання
2. **Які інструменти** ти б використовував (Figma, LLM, Cursor тощо)?
3. **Як саме** кожен інструмент ти б використовував?

---

## Питання 3 — Нейронні мережі в розробці

Як ти думаєш, наскільки доречне використання нейронних мереж при розробці?

- **Де** ти б їх використовував і **як саме**?
- **Де** ти б можливо **не використовував** їх, або використовував лише для відповіді на запитання?
- Як ти ставишся до думки, що нейронки краще **взагалі не використовувати** при написанні коду — бо код, який вони генерують, **неякісний**, і його довше дебажити, ніж він економить часу?

---

## Питання 4 — Взаємодія з QA

Як ти ставишся до відділу QA?

1. Де **їхня** зона відповідальності, а де **твоя**?
2. В яких випадках може статися так, що **помилку допустили саме вони**, а не ти — у задачі, якою займався ти, але де це **не твоя зона відповідальності**?

---

## Питання 5 — CSS Centering

Назви **4 способи** відцентрувати `div` по горизонталі.
